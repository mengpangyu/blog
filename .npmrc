# Use a local pnpm store inside the repo for reproducible CI/dev environments if desired.
# If you prefer the global pnpm store, you can remove this.
# store-dir=.pnpm-store

# pnpm settings
shared-workspace-lockfile=true
